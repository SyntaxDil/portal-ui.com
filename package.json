{
  "name": "portal-ui-website",
  "version": "1.0.0",
  "description": "Professional website hosted on GoDaddy with Registration landing page",
  "main": "Registration.html",
  "scripts": {
    "start": "npm run serve",
    "serve": "live-server --port=8080 --open=Registration.html",
    "deploy": "node deploy.js",
    "deploy:jacked": "node deploy.js --target jacked",
    "deploy:jacked:print": "node deploy.js --target jacked --print",
    "deploy:jacked:custom": "node deploy.js --local ./Sites/JackedDnb/site --remote /public_html/Sites/JackedDnb",
    "deploy:templedjs": "node deploy.js --target templedjs",
  "deploy:backbones-templedjs": "node deploy.js --target templedjs_backbones",
    "build": "echo 'No build step needed for static site'",
    "test": "echo 'No tests configured yet'"
  },
  "keywords": [
    "website",
    "godaddy",
    "registration",
    "portal"
  ],
  "author": "Portal UI Team",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "ftp-deploy": "^2.4.7",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
